import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import * as serviceWorker from './serviceWorker';

// This is where the initial render of our application happens, meaning the initial place it is called to be put on our page
ReactDOM.render( < App /> , document.getElementById('root'));
